import{BadRequestException,Injectable}from"@nestjs/common";import{randomUUID}from"node:crypto";import{PrismaService}from"../prisma/prisma.service";import type{CreateFileDto}from"./files.dto";
const ALLOWED=new Set(["image/jpeg","image/png","image/webp","application/pdf"]);@Injectable()export class FilesService{constructor(private readonly prisma:PrismaService){}create(ownerId:string,dto:CreateFileDto){if(!ALLOWED.has(dto.mimeType))throw new BadRequestException("UNSUPPORTED_FILE_TYPE");return this.prisma.fileAsset.create({data:{...dto,ownerId,objectKey:`private/${ownerId}/${randomUUID()}`}});}}
