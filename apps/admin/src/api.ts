export type ApiError={code:string;message:string;status:number};let refreshInFlight:Promise<string|null>|null=null;
async function refresh(){const token=sessionStorage.getItem("mc-admin-refresh");if(!token)return null;refreshInFlight??=fetch(`${import.meta.env.VITE_API_BASE_URL}/auth/refresh`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({refreshToken:token})}).then(async r=>{if(!r.ok)return null;const data=await r.json() as{accessToken:string;refreshToken:string};sessionStorage.setItem("mc-admin-token",data.accessToken);sessionStorage.setItem("mc-admin-refresh",data.refreshToken);return data.accessToken;}).finally(()=>{refreshInFlight=null;});return refreshInFlight;}
export async function apiRequest<T>(path:string,init:RequestInit={}){const execute=(token:string|null)=>fetch(`${import.meta.env.VITE_API_BASE_URL}${path}`,{...init,signal:init.signal??AbortSignal.timeout(15_000),headers:{"Content-Type":"application/json","Authorization":token?`Bearer ${token}`:"","X-Request-Id":crypto.randomUUID(),"X-Locale":localStorage.getItem("mc-locale")??"zh-CN","X-Timezone":Intl.DateTimeFormat().resolvedOptions().timeZone,...init.headers}});let response=await execute(sessionStorage.getItem("mc-admin-token"));if(response.status===401){const token=await refresh();if(token)response=await execute(token);}if(!response.ok)throw{code:"REQUEST_FAILED",message:response.statusText,status:response.status} satisfies ApiError;return response.json() as Promise<T>;}
