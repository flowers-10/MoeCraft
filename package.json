{
  "name": "moecraft",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "predev": "lsof -ti TCP:3100 -sTCP:LISTEN | xargs kill -9 2>/dev/null; lsof -ti TCP:3101 -sTCP:LISTEN | xargs kill -9 2>/dev/null; lsof -ti TCP:3102 -sTCP:LISTEN | xargs kill -9 2>/dev/null; true",
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "format:check": "node scripts/check-format.mjs",
    "lint": "node scripts/check-static.mjs",
    "check:migrations": "node scripts/check-migrations.mjs",
    "api:functional-smoke": "node scripts/api-functional-smoke.mjs",
    "audit:check": "pnpm audit --audit-level high",
    "secrets:check": "node scripts/check-secrets.mjs",
    "capacity:smoke": "node scripts/api-capacity-smoke.mjs",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules"
  },
  "devDependencies": {
    "turbo": "^2.5.5",
    "typescript": "^5.8.3"
  }
}
