<script setup lang="ts">import type{UiTableColumn}from"../types";defineProps<{columns:UiTableColumn[];rows:Record<string,unknown>[];rowKey?:string;emptyText?:string}>();</script>
<template><div class="table-wrap"><table><thead><tr><th v-for="column in columns" :key="column.key" :style="{width:column.width,textAlign:column.align}">{{column.label}}</th></tr></thead><tbody><tr v-for="(row,index) in rows" :key="String(row[rowKey??'id']??index)"><td v-for="column in columns" :key="column.key" :style="{textAlign:column.align}"><slot :name="`cell-${column.key}`" :row="row" :value="row[column.key]">{{row[column.key]}}</slot></td></tr><tr v-if="!rows.length"><td class="empty" :colspan="columns.length">{{emptyText||'—'}}</td></tr></tbody></table></div></template>
<style scoped>.table-wrap{width:100%;overflow:auto}table{width:100%;border-collapse:collapse;color:var(--text-secondary,#596174);font-size:12px}th{padding:11px 13px;background:var(--surface-raised,#f5f7fb);color:var(--text-muted,#7e8798);font-size:10px;font-weight:650;text-align:left}td{padding:13px;border-bottom:1px solid var(--border,#dce1eb)}tbody tr:hover{background:color-mix(in srgb,var(--accent-soft,#e3e7ff) 35%,transparent)}.empty{text-align:center;color:var(--text-muted,#7e8798)}</style>
